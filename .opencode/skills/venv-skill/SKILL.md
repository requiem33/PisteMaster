---
name: venv-skill
description: activate python virtual environment. use this whenever before a python-related commands are executed
---

# Python Virtual Environment

The project uses the `PisteMaster/venv` virtual environment.

**Before running any Python-related commands, activate it:**

```bash
source ./venv/bin/activate
```