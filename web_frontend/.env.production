# For development with Vite proxy, use /api (no VITE_API_BASE_URL set)
# This allows the Vite proxy to forward /api/* to the backend
# VITE_API_BASE_URL=/api

# For production builds without proxy, uncomment and set to your backend URL:
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
