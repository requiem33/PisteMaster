# Minimal requirements for desktop build (PyInstaller bundle)
# PySide6 and other GUI packages excluded - not needed for Django backend

# Django and REST framework
Django==4.2.*
djangorestframework==3.16.1
django-cors-headers==4.2.0
django_filter==22.1

# Database (SQLite for desktop)
# No psycopg needed - desktop uses SQLite

# Utilities
PyYAML==6.0.1
python-dotenv==1.0.0
python-dateutil==2.9.0
openpyxl==3.1.4
pypdf==6.4.0
requests==2.32.5

# Build tool
pyinstaller>=6.0.0