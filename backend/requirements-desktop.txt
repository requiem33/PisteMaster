# Minimal requirements for desktop build (PyInstaller bundle)

# Django and REST framework
Django==4.2.*
djangorestframework==3.16.1
django-cors-headers==4.2.0
django_filter==22.1

# Build tool
pyinstaller>=6.0.0